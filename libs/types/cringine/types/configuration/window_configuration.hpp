#pragma once

#include <string>

namespace cringine::types::configuration
{
    struct window_configuration
    {
        int width;
        int height;
        std::string name;
    };
} // namespace cringine::types::configuration
